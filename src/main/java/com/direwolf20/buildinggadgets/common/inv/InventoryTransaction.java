package com.direwolf20.buildinggadgets.common.inv;

/**
 * This class defines a system between the gadgets and the user. The user can build if they have the right
 * amount of items but the inventory system will create a transaction, remove the maximum amount of items
 * required for the build, then return any items that didn't get used.
 */
public class InventoryTransaction {
    public InventoryTransaction() {
        
    }
}
